# 渲染与交互实验室 QA 记录

- 日期：2026-07-16
- 分支：`codex/platform-complete-upgrade`
- 公开路由：`/docs/reference/experience-lab/complete-showcase/`
- 预览服务：仅保留 `3010`，由静态导出目录 `out/` 提供

## 自动化验收

- `npm run check`：内容校验、全部 Vitest、ESLint、类型检查、静态构建与导出产物校验。
- 组件覆盖：MDX 注册、媒体框、图片灯箱、图片组、前后对比、视频、数据表、公式、图表与下载卡。
- 边界覆盖：禁止恢复基于 `100vw - 固定导航宽度` 的扩宽算法；宽幅媒体默认限制在文章轨道内，只在 90rem 与 112rem 两档安全空间中渐进扩宽。
- 内容覆盖：实验室分类、展厅 frontmatter、31 项能力标记、公开资源与左侧知识树入口。

## 浏览器验收

真实浏览器在 561 × 834 的移动视口完成以下检查：

- 页面 `scrollWidth = 546`，无页面级横向滚动。
- 原有文章的 Mermaid 框边界为 `left 16 / right 530 / width 514`，没有进入导航区域。
- 展厅包含 8 个媒体框、Mermaid、数据图、图片组、前后对比、视频、状态条和完整 H2/H3/H4 层级。
- 左侧知识树可找到“渲染与交互实验室”和“完整渲染能力展厅”。
- Fumadocs 页签、代码页签、折叠区、图片灯箱、1.25× 缩放、Mermaid 全屏与关闭均可操作。
- 灯箱关闭后焦点回到原触发按钮。
- 前后对比滑块可获得焦点，按 `ArrowRight` 从 50% 更新到 51%；同时支持 Home / End 和四个方向键。
- 深色主题背景 `rgb(18, 16, 24)` 与浅色主题背景 `rgb(246, 242, 236)` 可真实切换。
- 页面交互完成后浏览器控制台日志为空。

## 资源与输出

以下资源均通过本地静态服务器返回 HTTP 200：

- 实验室首页与完整展厅路由
- `evidence-matrix.csv`
- `task-contract.json`
- `knowledge-flow.mp4`
- `knowledge-flow.zh-CN.vtt`
- `knowledge-flow-transcript.md`

短片为 1280 × 720、30fps、10 秒 H.264 MP4；默认不自动播放、不预加载整段文件，并提供海报、中文字幕、文字稿和直接文件入口。

## 结论

展厅已经可以同时承担读者示例、作者组件手册和长期视觉回归页。Mermaid 的宽幅表现改为内容轨道优先，不再依赖浏览器视口减去假定导航宽度，因此能避免特定缩放比例下侵入左侧知识树。
