# Design QA — Homepage Product Optimization

## 当前交付

- 静态预览：`http://localhost:3002/?v=18`
- 方向：AI Knowledge Universe / 产品化知识入口
- 覆盖：1280px 桌面、375px 手机、深浅主题、动效控制、按需实验、文档入口

## 已验证体验

- 首页先回答“今天想完成什么”，再提供精选内容、能力地图和可选实验。
- 首屏只启动 Three.js 品牌场景；实验区初始画布数量为 0。
- Spline、Rive、MapLibre 改为用户点击后才请求和挂载，未选择时不执行第三方实时引擎。
- Rive 实验点击后正常创建画布，无 loading 残留和错误边界触发。
- 动效开关状态可持久化；首次访问尊重 `prefers-reduced-motion`，页面可进入 `ready` 或 `disabled` 状态。
- 页面失焦后 Three.js 暂停绘制，恢复可见后继续。
- 深浅主题会同步更新首页、导航和实时实验区域。
- 1280px 与 375px 均无横向溢出；移动端目标与能力卡片采用双列，精选内容使用横向吸附浏览。
- 首页初始加载以及 Rive 实验启动后的控制台均无 error 或 warning。
- 文档页保留 Fumadocs 阅读结构，并使用同一品牌紫色、导航标识和主题变量。

## 设计取舍

- 删除首页默认展示的 ECharts 与 Matter.js 实验，降低页面长度与首屏认知负担。
- 将六个能力领域压缩为索引，不再让每张卡片承担主视觉角色。
- 把作者与开源信任信息移到精选内容之后，并在后段保留完整联系方式。
- 实时引擎作为可选“实验室”，不再阻挡用户进入真实教程内容。

## 验证结果

- [x] `npm run lint`
- [x] `npm run types:check`
- [x] `npm run build`
- [x] 静态导出成功
- [x] 初始仅 1 个首屏画布，实验画布为 0
- [x] Rive 按需加载后实验画布为 1
- [x] 深浅主题切换生效
- [x] 桌面与手机无横向溢出
- [x] 控制台无错误和警告

final result: passed
